# Audit 2026-06-04

Ежедневная сводка изменений за 2026-06-04. Файл содержит только изменения,
зафиксированные в этот день.

## Documentation rule

- Новые audit-сводки фиксируются в файле вида `docs/audit-2026-MM-DD.md`.
- `docs/audit-2026-06-02.md` оставлен только для изменений 2026-06-02.
- Изменения 2026-06-03 вынесены в `docs/audit-2026-06-03.md`.

## Epic 3.3: backend decomposition

**Сводка Epic 3.3.6 (HTTP server bootstrap extraction):**
- `backend/src/server.js`: 1 158 → **81 nonblank строк**.
- **`backend/src/app.js`** (новый, 1 090 nonblank строк): `createApp(...)`, Express middleware/routes wiring, health/readiness endpoints and global upload/body error adapter.
- `server.js` теперь отвечает только за `initDatabase()`, `app.listen(...)`, socket tracking, signal/IPC handlers and Redis-aware graceful shutdown.
- Verification: `node --check backend/src/server.js` + `node --check backend/src/app.js` clean.
- Smoke: `npm --prefix backend run smoke:health`, `smoke:shutdown`, `smoke:observability`, full `npm --prefix backend run smoke`.

**Сводка Epic 3.3.7 (users routes extraction):**
- `backend/src/app.js`: 1 090 → **1 013 nonblank строк** (−77, нетто).
- **`backend/src/routes/users.js`** (новый, 118 nonblank строк): tenant user list/detail/create/update/delete routes.
- `app.js` теперь подключает `registerUserRoutes(...)`; direct `bcrypt` import удалён из `app.js`.
- Verification: `node --check backend/src/app.js` + `node --check backend/src/routes/users.js` clean.
- Smoke: `npm --prefix backend run smoke:auth`, full `npm --prefix backend run smoke`.

**Сводка Epic 3.3.8 (company usage routes extraction):**
- `backend/src/app.js`: 1 013 → **767 nonblank строк** (−246, нетто).
- **`backend/src/routes/companyUsage.js`** (новый, 279 nonblank строк): tenant company usage summary and service notification recipient routes.
- `app.js` теперь подключает `registerCompanyUsageRoutes(...)`; route-local subscription summary, service warnings, notification recipients and tenant audit-log helpers вынесены из `app.js`.
- Verification: `node --check backend/src/app.js` + `node --check backend/src/routes/companyUsage.js` clean.

**Сводка Epic 3.3.9 (settings / reference routes extraction):**
- `backend/src/app.js`: 767 → **739 nonblank строк** (−28, нетто).
- **`backend/src/routes/settings.js`** (новый, 31 nonblank строк): tenant settings get/update routes.
- **`backend/src/routes/photo-requirements.js`**: 107 → 125 nonblank строк; reference endpoints `/api/photo-requirements/:type` and `/api/defect-categories` теперь регистрируются рядом со справочными константами.
- Verification: `node --check backend/src/app.js` + `node --check backend/src/routes/settings.js` + `node --check backend/src/routes/photo-requirements.js` clean.

**Сводка Epic 3.3.10 (protected uploads route extraction):**
- `backend/src/app.js`: 739 → **718 nonblank строк** (−21, нетто).
- **`backend/src/routes/uploads.js`** (новый, 37 nonblank строк): authenticated `/uploads/*` route with tenant photo ownership checks.
- `app.js` теперь подключает `registerProtectedUploadRoutes(...)`; direct imports `getMimeType`, `buildUploadUrl`, `resolveUploadPath` удалены из `app.js`.
- Verification: `node --check backend/src/app.js` + `node --check backend/src/routes/uploads.js` clean.

**Сводка Epic 3.3.11 (company policy service extraction):**
- `backend/src/app.js`: 718 → **613 nonblank строк** (−105, нетто).
- **`backend/src/services/companyPolicy.js`** (новый, 130 nonblank строк): company limits, resource usage, feature flags and subscription write-guard helpers.
- `app.js` теперь создаёт `companyPolicy` через `createCompanyPolicy({ db, sendError, API_MESSAGES })` и передаёт прежние callbacks в route-модули.
- Verification: `node --check backend/src/app.js` + `node --check backend/src/services/companyPolicy.js` clean.

**Сводка Epic 3.3.12 (role guards service extraction):**
- `backend/src/app.js`: 613 → **581 nonblank строк** (−32, нетто).
- **`backend/src/services/roleGuards.js`** (новый, 60 nonblank строк): role predicates, authorization guards and assignable tenant-user role helpers.
- `app.js` теперь создаёт `roleGuards` через `createRoleGuards({ sendError, API_MESSAGES })` и передаёт прежние callbacks в route-модули.
- Verification: `node --check backend/src/app.js` + `node --check backend/src/services/roleGuards.js` clean.

**Сводка Epic 3.3.13 (user store service extraction):**
- `backend/src/app.js`: 581 → **545 nonblank строк** (−36, нетто).
- **`backend/src/services/users.js`** (новый, 54 nonblank строк): user lookup/create/update DB helpers.
- `app.js` теперь создаёт `userStore` через `createUserStore({ db })` и передаёт прежние callbacks в auth/users/seed route-модули.
- Verification: `node --check backend/src/app.js` + `node --check backend/src/services/users.js` clean.

**Сводка Epic 3.3.14 (health routes extraction):**
- `backend/src/app.js`: 545 → **469 nonblank строк** (−76, нетто).
- **`backend/src/routes/health.js`** (новый, 89 nonblank строк): liveness/readiness endpoints, DB/uploads/Redis readiness checks and shutdown guard.
- `app.js` теперь подключает `registerHealthRoutes(...)`; direct imports `path`, `fs`, Redis status helpers and `uploadsDir` удалены из `app.js`.
- Verification: `node --check backend/src/app.js` + `node --check backend/src/routes/health.js` clean.

## Current state

- Branch: `restore-yesterday-full`.
- Latest completed backend decomposition chunks:
  - Epic 3.3.6: HTTP server bootstrap extraction.
  - Epic 3.3.7: users routes extraction.
  - Epic 3.3.8: company usage routes extraction.
  - Epic 3.3.9: settings / reference routes extraction.
  - Epic 3.3.10: protected uploads route extraction.
  - Epic 3.3.11: company policy service extraction.
  - Epic 3.3.12: role guards service extraction.
  - Epic 3.3.13: user store service extraction.
  - Epic 3.3.14: health routes extraction.
- Current backend counts:
  - `backend/src/server.js`: 81 nonblank строк.
  - `backend/src/app.js`: 469 nonblank строк.
  - `backend/src/routes/users.js`: 118 nonblank строк.
  - `backend/src/routes/companyUsage.js`: 279 nonblank строк.
  - `backend/src/routes/settings.js`: 31 nonblank строк.
  - `backend/src/routes/photo-requirements.js`: 125 nonblank строк.
  - `backend/src/routes/uploads.js`: 37 nonblank строк.
  - `backend/src/services/companyPolicy.js`: 130 nonblank строк.
  - `backend/src/services/roleGuards.js`: 60 nonblank строк.
  - `backend/src/services/users.js`: 54 nonblank строк.
  - `backend/src/routes/health.js`: 89 nonblank строк.

## Verification

- `git diff --check`
- `npm --prefix backend run smoke`
- `npm --prefix backend run test:unit` — 41 passed, 0 failed
