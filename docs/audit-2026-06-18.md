# Audit report — 2026-06-18

## Сводка дня

- Продолжена подготовка проекта к пилоту после P0/P1-реализации: фокус дня —
  стабилизация resource-admin backend, декомпозиция большого route-файла и
  фиксация проверок.
- Основной технический результат: `backend/src/routes/adminSaas.js` уменьшен
  с 2207 строк в начале серии декомпозиции до 642 строк без изменения
  публичного resource-admin HTTP-контракта.
- Backend unit suite расширен до 81 теста. Полный backend smoke-набор,
  `smoke:saas-admin`, lint и `git diff --check` проходят.

## Resource-admin backend decomposition

### Subscription alerts

- Из `adminSaas.js` выделен сервис
  `backend/src/services/subscriptionAlerts.js`.
- В сервис перенесены правила статусов подписок, окна уведомлений 14/7/3/1
  день, grace/expired/suspended логика, дедупликация сервисных уведомлений и
  выбор tenant-получателей.
- CLI `npm --prefix backend run subscriptions:check` теперь использует сервис
  напрямую, не импортируя HTTP route-модуль.
- Добавлены unit-тесты переходов `active`, `expiring`, `grace`, `expired` и
  `suspended`.

### Billing, plans и owners

- Payment mapping, payment queries, summary платежей, MRR и перерасчёт
  `company_subscriptions` вынесены в
  `backend/src/services/resourceBilling.js`.
- Mapping тарифов, create/update payload builders и upsert лимитов компаний
  вынесены в `backend/src/services/resourcePlans.js`.
- Owner mapping, setup-link status, выборки и мутации владельцев компаний
  вынесены в `backend/src/services/resourceOwners.js`.
- Route-слой сохранил permission checks, audit log, HTTP-валидацию и форматы
  ответов.

### Companies, activity и insights

- Реестр компаний, usage/billing/subscription mapping, health/risk helpers и
  проверки наличия tenant-данных вынесены в
  `backend/src/services/resourceCompanies.js`.
- Resource/company notifications, audit-log mapping, safe JSON parsing и
  отметка уведомления прочитанным вынесены в
  `backend/src/services/resourceActivity.js`.
- Limit usage, disabled modules, churn/upsell risk center и billing summary
  вынесены в `backend/src/services/resourceInsights.js`.

### Dashboard

- Dashboard SQL-агрегаты totals, plan breakdown, activity trend, product
  activity, storage, OCR stats, service health, health center и activation
  funnel вынесены в
  `backend/src/services/resourceDashboard.js`.
- `/api/admin/saas/stats` сохраняет прежний контракт: route-слой теперь
  собирает итоговый response из сервисов, но не содержит aggregate queries.
- Добавлены unit-тесты mapper-ов totals, product activity, storage stats,
  health items и activation funnel в
  `backend/tests/unit/resource-dashboard.test.mjs`.

### Stats composition

- Сборка общего `/api/admin/saas/stats` response и карточки компании вынесена
  в `backend/src/services/resourceAdminStats.js`.
- Route-файл сохранил HTTP endpoints, permission checks, audit writes и
  route-level валидацию. Старые named exports сохранены через re-export.
- Добавлен unit-тест `buildResourceCompanyDetails` в
  `backend/tests/unit/resource-admin-stats.test.mjs`.

## P0/P1 feature state in working tree

- P0 надёжного доказательного осмотра остаётся в рабочем дереве: durable
  mobile draft, очередь фото, readiness, immutable completed inspection,
  watermark WebP и авторизованный PDF с SHA-256/размером.
- P1 согласования осмотра остаётся в рабочем дереве: отдельный
  `approval_status`, история переходов, RBAC, уведомления и отображение в PDF.
- P1 план-графика осмотров остаётся в рабочем дереве: интервалы компании,
  переопределения техники, единый расчёт рисков, фильтры и web UI.
- P1 lifecycle дефекта остаётся в рабочем дереве: критичность,
  управленческие статусы, обязательный комментарий, история и уведомления.

## Документация и журналы

- Обновлены `CHANGELOG.md`, `plan.md` и
  `docs/audit-2026-06-12.md` с фактическими шагами декомпозиции.
- Создан этот отдельный дневной audit-файл для изменений за 2026-06-18.

## Проверки

- `npm --prefix backend run lint` — пройден.
- `npm --prefix backend run test:unit` — 81 тест пройден.
- `npm --prefix backend run smoke:saas-admin` — пройден.
- `npm --prefix backend run smoke` — пройден полностью.
- `npm run verify:launch` — пройден полностью: backend/web/mobile lint,
  81 backend unit-тест, полный backend smoke, web production build, mobile
  verify/EAS readiness, 11 Chromium E2E, launch doctors и dependency audits.
- `npm run backup:local` — пройден; создан backup
  `backend/backups/2026-06-18T10-37-51-316Z`.
- `npm run backup:verify` — пройден; SQLite integrity `ok`, uploads проверены.
- `npm run release:readiness` — пройден со статусом
  `pilot-ready-after-release-actions`. Структурных blockers нет; до release
  commit отчёт ожидаемо фиксировал dirty working tree.
- `npm run release:evidence` — пройден; создан локальный manifest
  `release-evidence/release-evidence-2026-06-18T10-38-02-978Z.json`.
- `npm run release:first-start` — пройден как read-only operator checklist;
  подтвердил отсутствие локальных `backend/.env.production` и
  `web/.env.production`, которые должны быть настроены на pilot/staging host.
- `npm run mobile:eas:readiness` — пройден, статус `ready`; документация
  подтверждает, что `EXPO_PUBLIC_API_URL` должен быть задан в EAS environment
  variables, а не только в локальном `mobile/.env.production`.
- `npm run doctor:production` — заблокирован локально из-за отсутствующего
  `backend/.env.production` и не заданных production persistent paths
  (`DATABASE_PATH`, `UPLOAD_DIR`, `BACKUP_DIR`, `TRUST_PROXY`, `CORS_ORIGINS`,
  `JWT_SECRET`). Это внешний blocker для pilot/staging, а не локально
  замаскированный pass.
- `git diff --check` — пройден; выводит только ожидаемые Windows line-ending
  warnings `LF will be replaced by CRLF`.
- В полном smoke сохраняется ожидаемое предупреждение обработки повреждённого
  SVG-буфера: тест подтверждает безопасный отказ и не считается ошибкой.
- После одного флейка полного `verify:launch` на ожидании `smoke:public-demo`
  окно ожидания старта smoke-сервера увеличено до 60 секунд; изолированный
  `smoke:public-demo` проходил, продуктовая логика не менялась.

## RC status

- Локальные release gates на 2026-06-18 пройдены после фиксации web/mobile
  dependency audit через overrides для транзитивных `@babel/core`, `js-yaml`,
  `tmp` и `ws`.
- Функциональный freeze подтверждён: крупную декомпозицию больше не расширять
  до прохождения внешнего pilot/staging `doctor:production`, кроме точечных
  исправлений найденных gate failures.
- Текущий статус: release candidate готов к pilot/staging проверке после
  release commit и подготовки реального production env.

## Working tree grouping

- P0/P1 inspection evidence: backend routes/services для readiness,
  immutable completion, reports, approval, schedule и defect lifecycle;
  web inspection/detail/settings UI; mobile durable draft/photo queue.
- Subscription/resource-admin: декомпозиция `adminSaas.js` в сервисы
  `subscriptionAlerts`, `resourceBilling`, `resourcePlans`, `resourceOwners`,
  `resourceCompanies`, `resourceActivity`, `resourceInsights`,
  `resourceDashboard` и `resourceAdminStats` с unit-тестами.
- Release hardening: web/mobile dependency overrides и lockfiles, backup,
  release readiness/evidence, first-start checklist, production-env/runbook
  сверка.
- Documentation: `CHANGELOG.md`, `plan.md`, `docs/audit-2026-06-12.md`,
  `docs/audit-2026-06-18.md`, feature notes для reliable inspection,
  approval, schedule и defect lifecycle.
- Runtime artifacts: `backend/backups/`, `backend/data/`, `backend/uploads/`
  и `release-evidence/` остаются ignored и не входят в release commit.

## Оставшиеся действия перед пилотом

- Запустить `npm run doctor:production` на реальном pilot/staging окружении и
  получить `ok: true`, `errors: []`.
- Создать и проверить backup уже на pilot/staging данных.
- Приложить release evidence JSON к release notes.
- Собрать mobile EAS artifact.
