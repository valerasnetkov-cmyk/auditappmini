# Audit report - 2026-06-30

## Исправления по аудиту проекта

- Исправлен Redis-unavailable fallback rate limiter: при недоступном Redis
  middleware больше не создает новый in-memory limiter на каждый запрос и
  сохраняет счетчики в рамках процесса.
- Добавлен regression-тест для fallback limiter, который проверяет повторный
  запрос после исчерпания лимита.
- Интеграционные тесты backend приведены к текущему контракту
  `/api/health/ready`: при отсутствующем backend они помечаются skipped, а при
  запущенном backend проверяют `status: "ok"` и `ready: true`.
- Mobile dependency audit закрыт через override `undici@^6.27.0` в
  `mobile/package.json` и обновленный `mobile/package-lock.json`.
- Новые фото осмотров, дефектов, primary photo техники и public demo seed теперь
  сохраняются в tenant-scoped дереве:
  `/uploads/companies/{companyId}/inspections/{inspectionId}/photos/{photoId}/...`.
- Документация photo pipeline обновлена под tenant-scoped storage layout.
- Добавлен regression-тест `backend/tests/unit/photo-upload-path.test.mjs`,
  который проверяет URL и физические файлы `main.webp`, `original.*`,
  `thumb.webp` под сегментом компании.
- Перепроверен старый audit-пункт про N+1 в `GET /api/vehicles`: текущая
  реализация уже использует CTE `latest_inspections` и `defect_counts`, а
  `smoke:vehicles` проверяет `lastInspection` и `defectsCount`.
- Перепроверены старые security backlog-пункты MFA/owner setup: явный
  `/api/users/:id/mfa/enable`, login challenge `/api/auth/mfa/verify`,
  `/api/users/:id/mfa/disable` и одноразовый owner setup nonce уже реализованы;
  `smoke:mfa` и `smoke:owner-setup` входят в full backend smoke.
- Сужена задача `docs/mobile-add-vehicle-and-quick-inspection.md`: создание авто
  инспектором из mobile больше не входит в scope; реализуется только запрет
  выбора доказательных фото как обложки техники.
- Web-карточка техники теперь получает через `/api/vehicles/:id/photo-options`
  только `photo_type = 'overall'`, а `/primary-photo/from-photo` отклоняет
  любые не-overall фото без раскрытия их допустимости.
- Внешний operational pack из `C:/Projects/Auditmini/Новая папка` интегрирован
  в основную документацию проекта: добавлены process map, worker target,
  observability/alerts, resource-admin processes и client success/support.
- Выбран безопасный порядок реализации: сначала observability, release evidence
  и resource-admin visibility; полный worker queue слой и async photo/PDF
  вынесены в следующий этап.
- Добавлен backend alert service с dry-run режимом, Sentry-ready env
  placeholders и `npm --prefix backend run alerts:dry-run`; smoke observability
  проверяет request id, readiness shape, dry-run алерт и маскирование секретов.
- Resource-admin health center расширен service-level operational status:
  backup manifest, Redis/uploads, alerts и worker placeholder `not_configured`
  без чтения tenant endpoints.
- Release readiness/evidence теперь фиксируют alert dry-run command,
  Sentry/Telegram env presence без секретов и worker status `not_configured`.

## Verification

- `npm --prefix backend run test:unit` — passed, 93 tests.
- `npm --prefix backend run test:integration` без запущенного backend — passed,
  3 skipped.
- `npm --prefix backend run test:integration` с временным backend и seed owner —
  passed, 3 tests.
- `npm --prefix backend run lint` — passed.
- `npm --prefix backend run smoke:inspections` — passed, `ok: true`
  (warning about corrupt header belongs to the negative invalid-photo check).
- `npm --prefix backend run smoke:vehicles` — passed, `ok: true`.
- `npm --prefix backend run smoke:vehicles` после фильтрации overview-фото —
  passed; smoke проверяет, что `photo-options` возвращает только `overall`,
  `odometer`-фото отклоняется, а `overall` можно назначить основным.
- `npm --prefix backend run alerts:dry-run` — passed; dry-run возвращает
  `ok: true`, `dryRun: true`, `delivered: false`, секретный `token` в context
  маскируется.
- `npm --prefix backend run smoke:observability` после operational integration —
  passed; проверены request id echo, readiness shape, structured access log,
  skip paths и alert dry-run.
- `npm --prefix backend run smoke:health` — первый запуск попал в внешний
  `PORT` окружения и получил HTML вместо JSON; повтор с явным тестовым
  `PORT=15417` passed.
- `backend/scripts/smoke-health.mjs` больше не зависит от внешнего `PORT` и
  сам выделяет свободный порт; повтор с загрязнённым `PORT=1` — passed.
- `npm --prefix backend run smoke:saas-admin` после resource-admin health
  extension — passed.
- `npm --prefix backend run lint` после alert/operational changes — passed.
- `npm --prefix web run lint` после type/env/docs integration — passed.
- `npm --prefix web run build` после type/env/docs integration — passed.
- `npm run release:readiness` — passed; отчёт показывает новый observability
  block, alert dry-run command и `workers: not_configured` как ожидаемый
  текущий статус.
- `npm run release:evidence -- --dry-run` — passed; JSON содержит
  `observability` с Sentry/Telegram presence и worker placeholder.
- `npm run release:evidence` — passed; generated
  `release-evidence/release-evidence-2026-06-30T09-33-23-662Z.json`
  (ignored runtime evidence).
- `git diff --check` — passed with expected Windows LF→CRLF warnings only.
- `graphify update .` — passed; AST graph rebuilt, graphify notes that doc
  semantic update requires full `/graphify --update` if needed.
- `npm run verify:launch` после operational integration и smoke-health
  hardening — passed end to end: root lint, backend unit/full smoke, web build,
  mobile verify, isolated Chromium E2E, launch doctors and dependency audits.
  Expected negative invalid-photo smoke warning:
  `[uploads] Failed to process inspection photo: Input buffer has corrupt header`.
- Добавлен worker foundation без перевода photo/PDF в async: SQLite-backed
  `worker_jobs`, `worker_heartbeats`, job service с enqueue/claim/complete/fail,
  `worker:run-once` для `billing.scan` и `smoke:workers`.
- Resource-admin operational status теперь читает реальную очередь/heartbeat:
  `idle` без job evidence, `running` при успешном worker heartbeat,
  `degraded` при failed jobs за 24 часа или ошибке чтения статуса.
- Release readiness/evidence обновлены с `workers.status = foundation_available`,
  `npm --prefix backend run worker:run-once` и
  `npm --prefix backend run smoke:workers`.
- `npm --prefix backend run smoke:workers` — passed; проверены idempotency key,
  выполнение `billing.scan`, `completed` job status и heartbeat `smoke-worker`.
- `npm --prefix backend run smoke:saas-admin` после подключения queue status в
  operational health — passed.
- `npm run release:readiness` после worker foundation — passed; отчёт показывает
  `Workers: foundation_available`, `worker:run-once` и `smoke:workers`.
- `npm run release:evidence -- --dry-run` после worker foundation — passed;
  JSON содержит `observability.workers.status = foundation_available`.
- `npm --prefix backend run lint` после worker foundation — passed.
- `npm --prefix backend run test:unit` после worker foundation — passed,
  93 tests.
- `npm --prefix backend run smoke` после worker foundation — passed, full backend
  smoke suite including `smoke:workers`; expected invalid-photo warning remains
  the negative corrupt-header test.
- Добавлен local storage abstraction: `backend/src/services/storage.js`,
  `STORAGE_DRIVER=local`, `uploadStorage` для upload path resolution и readiness
  probe, без изменения URL/tenant contracts.
- Реализован первый слой resource-admin support center: в карточке компании
  добавлены внутренние support notes, следующий шаг и последний контакт через
  существующий admin update/details flow, без чтения tenant endpoints.
- `npm --prefix backend run smoke:storage` — passed; проверены
  save/read/verify/delete, `/uploads/...` local URL shape и rejection traversal
  key.
- `npm --prefix backend run smoke:saas-admin` после support notes — passed;
  smoke проверяет сохранение полей сопровождения и их возврат в details.
- `npm --prefix backend run smoke:health` после storage abstraction — passed.
- `npm --prefix backend run smoke:vehicles` после storage abstraction — passed.
- `npm --prefix backend run smoke:public-demo` — passed, `ok: true`.
- `npm --prefix backend run smoke` — passed, full backend smoke suite.
- `npm --prefix backend run smoke` после фильтрации overview-фото — passed,
  full backend smoke suite.
- `npm --prefix backend audit --audit-level=moderate` — 0 vulnerabilities.
- `npm --prefix web audit --audit-level=moderate` — 0 vulnerabilities.
- `npm --prefix web audit --audit-level=high` — 0 vulnerabilities.
- `npm --prefix web run lint` — passed.
- `npm --prefix web run build` — passed.
- `npm --prefix web run lint` и `npm --prefix web run build` после изменения
  карточки техники — passed.
- `npm run doctor:launch` — passed for backend, web and mobile.
- `npm run backup:verify` — passed against the latest local backup.
- `npm run release:readiness` — no structural blockers; remaining required
  actions are release/operator gates for committing the exact version,
  production env validation, backup creation, first-start review and EAS build.
- `npm run release:evidence` — passed; generated a local ignored evidence JSON.
- `npm run verify:e2e` — passed, 11 Chromium tests.
- `npm --prefix mobile audit --audit-level=moderate` — 0 vulnerabilities.
- `npm --prefix mobile run typecheck` — passed.
- `npm --prefix mobile run lint` — passed.
- `npm --prefix mobile run verify` — passed, including lint, typecheck,
  install check, Expo doctor and EAS readiness.
- `npm run verify:launch` — passed end to end: root lint, backend unit/full
  smoke, web build, mobile verify, isolated Chromium E2E, launch doctors and
  dependency audits.
