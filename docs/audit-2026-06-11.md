# Audit report — 2026-06-11

## Публичное демо и актуализация лендинга

- Реализован публичный вход в изолированную тестовую компанию без регистрации.
- Демо-компания работает в режиме `demo_readonly`: чтение разрешено, изменение
  техники, осмотров, дефектов, пользователей, настроек и фотографий
  заблокировано.
- Добавлены 12 тестовых единиц техники, 36 осмотров, 16 дефектов, чек-листы,
  значения пробега, ДТП-сценарии и три сгенерированные демонстрационные
  фотографии.
- Данные не связаны с реальными компаниями, пользователями, техникой или
  происшествиями.
- Для недельного графика сформирована актуальная динамика из 21 осмотра:
  `2, 3, 4, 3, 5, 3, 1` по семи последовательным дням.
- В географии демо-парка наименование «Сахалинская область» заменено на
  «Невельск». Распределение техники: Южно-Сахалинск — 4, Невельск — 3,
  Корсаков — 3, Холмск — 2.
- Карточка осмотра на публичном лендинге переведена со статических дат и
  пробега на ежедневный серверный расчёт по часовому поясу `Asia/Sakhalin`.
- Дата следующего осмотра рассчитывается как текущая дата плюс семь дней,
  пробег увеличивается на 42 км в сутки, месячная динамика составляет
  1 260 км.
- На лендинг возвращён основной заголовок «Контроль автопарка без спорных фото
  и ручного хаоса».

## Проверка публичного демо

- `npm --prefix backend run smoke:public-demo` пройден: подтверждены компания
  `demo`, 12 единиц техники, 36 осмотров, режим `demo_readonly`, блокировка
  записи и защищённая выдача фотографий.
- API аналитики проверен напрямую: недельный период возвращает семь точек и
  21 осмотр, месячный период — 27 осмотров.
- Демо-вход и отображение аналитического дашборда проверены в браузере.
- `npx eslint src/app/page.tsx` пройден без ошибок.
- Серверная выдача лендинга проверена: отображаются текущая дата осмотра,
  рассчитанный пробег и дата следующего осмотра.

## Visual rebrand

- Reviewed `auditavto_visual_rebrand_replacements.zip` and applied it as a
  compatibility-focused update rather than a full file replacement.
- Added the supplied AuditAvto horizontal logos, light/dark marks and SVG
  favicon under `web/public/brand/`.
- Replaced the web design tokens with the supplied graphite and orange system,
  including light/dark values and evidence-specific semantic colors.
- Preserved the existing landing layout, pricing section and real hero login.
  Updated the first-screen message to focus on photo evidence before and after
  a trip, with demo-inspection and pilot CTAs.
- Updated the standalone login and authenticated sidebar to use the new brand
  assets.
- Added reusable evidence, inspection-step, defect-marker, odometer and report
  CSS primitives without forcing them into existing business screens.
- Added the supplied mobile theme source and connected it to the existing theme
  provider while keeping its public API unchanged.
- Replaced sidebar letter codes with semantic outline icons for dashboard,
  vehicles, inspections, defects, users and resource administration. Search
  and logout now use recognizable action icons, with clearer active states.
- Adjusted the landing vehicle preview crop to keep the inspected vehicle
  visually centered.
- Updated the landing vehicle preview to use contained image fitting so the
  complete vehicle remains visible inside the card.
- Removed the truck image frame and padding, retained a white background and
  fitted the full vehicle vertically. Reworked the adjacent inspection card
  with four compact information blocks: mileage, defects, technical condition
  and next inspection.

## Risk controls

- No backend, API, authentication, billing or routing behavior was changed.
- The archive `globals.css` was not copied wholesale; existing project styles
  were retained and only compatible primitives were merged.
- Existing blue status utilities in feature screens were not mass-rewritten,
  avoiding accidental loss of semantic distinction.

## Calm UI components

- Reviewed `animata-calm-ui-components.md` and used Animata only as a behavior
  reference; no package or copied decorative component code was added.
- Added a shared web UI layer with `Badge`, `NoticeCard`, `ProgressBar`,
  `Skeleton`, `EmptyState`, `StatusButton` and `Toast`.
- All component CSS lives in `web/src/components/ui/ui.css`, uses existing
  theme tokens and includes focused `prefers-reduced-motion` handling.
- Replaced the inline-styled global toast with a typed, closable,
  four-second notification that remains compatible with existing one-argument
  `showToast` calls.
- Replaced dashboard spinners and empty content with structural skeleton and
  empty-state components.
- Replaced inspection status/error banners, missing-data warnings,
  required-photo pills and save loading feedback with shared components.
- Replaced the remaining blue photo-upload spinners in required-photo and
  defect-photo controls with a token-based reduced-motion-aware indicator.
- Replaced the company usage inline-width bars with native accessible
  `<progress>` elements and unified resource/feature badges.
- Reused the shared toast on dashboard and inspection list pages.
- Added `Stepper`, `Accordion` and `Tooltip` to the shared UI layer.
- Added a seven-stage inspection readiness stepper covering vehicle, type,
  required photos, checklist, defect evidence, odometer/accident data and
  report readiness. States are derived from existing data and do not introduce
  new completion rules.
- Added required-photo progress as an accessible native progress element with
  completed/required counts.
- Migrated the landing FAQ to the shared native disclosure component and added
  a keyboard-focusable explanation for OCR in company usage settings.
- Migrated SaaS-admin subscription alerts to `StatusButton`, `NoticeCard`,
  `Skeleton`, `Badge` and shared `EmptyState` components.
- Migrated resource-dashboard loading, errors, activation status,
  health-center cards and upsell values to the shared semantic UI layer.
- Reworked the owner-facing subscription banner to use `NoticeCard` and
  `Badge`, retaining all billing labels, warning text and period calculations.
- Added semantic urgency to the planned-inspection notification count.
- Migrated plan and offline-payment pages to shared success/error notices,
  structural loading skeletons, semantic status badges and action-state
  buttons.
- Migrated company creation, owner creation and company-limit forms to
  `StatusButton` while preserving native form submission and validation.
- Migrated MFA list/detail loading, errors, success feedback, MFA status,
  setup/verification actions and missing-user states to the shared UI layer.
- Added an accessible tooltip for the MFA manual-secret fallback and removed
  the remaining inline layout style from that page.
- Migrated users, vehicles and defects list pages to shared structural
  skeletons, semantic notices, action-state buttons and status badges.
- Updated the user modal and vehicle form with shared form controls,
  restriction feedback and explicit saving states while preserving existing
  validation and CRUD behavior.
- Updated the defect detail loading state, tariff restriction notice,
  inspection/status badges and close/reopen actions to the calm UI layer.

## Verification

- `web`: `npm run lint` passed with three pre-existing `no-img-element`
  warnings in inspection components and no errors.
- `web`: `npm run build` passed.
- `mobile`: `npm run typecheck` passed.
- Browser review passed at `1440x1080` and `390x844`.
- Mobile document width matched the viewport (`390px`), with no horizontal
  overflow.
- Compared the desktop render with approved concept `001.png`: first-screen
  composition, login hierarchy, product cards, security cues and restrained
  heading scale remain aligned. Graphite/orange color and revised evidence-led
  copy are intentional rebrand deviations.
- Re-ran web lint and production build after navigation polish; both passed.
  Browser review confirmed the landing remains intact and unauthenticated
  `/dashboard` continues to render the protected login surface.
- After calm UI integration, `npm run lint` and `npm run build` passed. Lint
  retains three existing `no-img-element` warnings in inspection photo views.
- Public login rendering was browser-checked. The documented local owner
  credentials were not valid in the current database, so authenticated visual
  review was not forced by creating or changing local business data.
- Browser interaction confirmed FAQ disclosure opens and exposes its answer
  correctly. The second-stage lint and production build passed with the same
  three existing inspection-photo warnings.
- The SaaS-admin migration passed lint and production build. Automated admin
  login visual inspection was blocked by the in-app browser virtual clipboard
  layer; no auth state or business data was changed to bypass that limitation.
- Administration form and MFA migration passed lint and production build.
  Targeted source checks found no remaining inline styles, legacy blue submit
  buttons or old spinning loaders in the migrated paths.
