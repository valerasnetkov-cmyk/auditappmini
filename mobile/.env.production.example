# Auditmini mobile production environment template.
# Copy to mobile/.env.production before building a production Expo/native app
# or configure the same public value in your build pipeline.

# Public backend API URL used by the mobile client.
# Must be absolute, HTTPS in production, and end with /api.
EXPO_PUBLIC_API_URL=https://api.example.com/api
