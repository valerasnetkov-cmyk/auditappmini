# Auditmini web production environment template.
# Copy to web/.env.production or configure the same value in your hosting platform.
# Do not commit real production domains if the repository is public.

# Public backend API URL used by the browser client.
# Must be absolute, HTTPS in production, and end with /api.
NEXT_PUBLIC_API_URL=https://api.example.com/api
